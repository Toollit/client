import React from 'react';
import EmailAuthController from '@/models/temp-signUp/emailAuth/EmailAuthController';

const EmailAuth = () => {
  return <EmailAuthController />;
};

export default EmailAuth;
