import styled from '@emotion/styled';

const Button = styled.button`
  display: flex;
  padding: 0.5rem;
  cursor: pointer;
  border-style: none;
  background-color: transparent;
`;

export { Button };
